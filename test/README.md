# SSL testing

Using the `ssl_gen.sh` script, you will generate a `server.key` (private key file) and `server.crt` (certificate file).

The server uses both files. The client only uses the `server.crt` file.
